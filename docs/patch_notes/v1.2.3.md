# Patch Notes — v1.2.3

## Highlights
- Softened the FTIR functional-group overlays by shrinking correlation bands, scaling their colour intensity with classifier confidence, and capping on-plot annotations to reduce clutter.
- Wrapped the functional-group predictions table in a collapsible expander so the plot retains vertical space until analysts need tabular details.
- Bumped release metadata to capture the UI refinements for the IR classifier workflow.

## Verification
- `pytest tests/ml/test_ir_group_classifier.py -q`
