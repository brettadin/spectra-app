# Spectra App v1.2.1af — 2025-11-01

## Summary
- bundle a lightweight HDF5 linear surrogate for the IR functional-group classifier so offline sessions stay predictive without TensorFlow
- extend classifier loading to prefer TensorFlow weights, fall back to the linear surrogate, and only use the heuristic rules as a last resort
- add h5py to the environment, refresh FTIR UI messaging, documentation, and unit coverage for the new surrogate path

## Verification
- pytest tests/ml/test_ir_group_classifier.py -q
