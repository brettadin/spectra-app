# v1.2.1ae — IR classifier fallback

## Summary
- add a heuristic fallback when the TensorFlow FTIR model assets are missing so UI actions still succeed
- centralise functional-group correlation bands for the UI/ML layers and surface classifier backend state to users and the API
- extend unit coverage for the fallback path and document how to restore the pretrained weights

## Verification
- pytest tests/ml/test_ir_group_classifier.py -q
