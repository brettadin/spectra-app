# Spectra App v1.2.2 — 2025-11-02

## Summary
- repackage the offline IR surrogate as a gzipped/base64 JSON asset plus JSON thresholds so PR tooling can apply the classifier patch without binary blobs
- update the loader to decode the textual bundle ahead of the heuristic fallback and prefer TensorFlow/HDF5 weights when available
- refresh classifier tests, README/setup notes, and asset docs to cover the new bundle format

## Verification
- pytest tests/ml/test_ir_group_classifier.py -q
